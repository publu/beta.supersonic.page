import { useEagerConnect } from "../hooks";
import DefaultLayout from "../layouts/Default";

const Me: React.FC = () => {
  return (
    <DefaultLayout>
    </DefaultLayout>
  );
}

export default Me;